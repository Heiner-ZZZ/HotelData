# Dependencies owned by HotelData's GA03 DAG.
# Airflow itself and its celery/postgres providers are installed in the Dockerfile
# with the official Airflow 3.2.2 constraints before these custom packages.
pandas>=2.3,<2.4
pyarrow>=24,<25
pymongo>=4.17,<5.0
python-dotenv>=1.2,<2.0
requests>=2.34,<3.0
