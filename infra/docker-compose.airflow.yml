# Compatibility entry point.
# Use docker-compose.yml as the single source of truth for the Airflow 3
# deployment. This file no longer exposes the obsolete Airflow 2 standalone
# service with SQLite and SequentialExecutor.
include:
  - path: ./docker-compose.yml
