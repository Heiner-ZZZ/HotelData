# Compatibility entry point.
# The canonical Airflow 3 deployment is defined in docker-compose.yml.
# Compose includes preserve one source of truth and avoid accidentally starting
# the former single-container SequentialExecutor topology.
include:
  - path: ./docker-compose.yml
